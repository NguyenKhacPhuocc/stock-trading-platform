import { LayoutDashboard } from 'lucide-react';
import type { SidebarNavItem } from '@/components/sidebar';

export const ACCOUNT_NAV_ITEMS: SidebarNavItem[] = [
  { tabName: 'Tổng hợp tài sản', href: '/account', icon: LayoutDashboard },
];
