# AI service (FastAPI)
#
# Quy ước: sửa file này khi đổi biến môi trường trong code → đồng bộ .env (cùng thư mục).
#
# CORS trong main.py mặc định cho phép BACKEND_URL (Nest gọi vào đây).

BACKEND_URL=http://localhost:3001
