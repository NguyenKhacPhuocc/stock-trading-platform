/** Loại biến động khối lượng CP trên sổ sao kê chứng khoán. */
export enum StockLedgerType {
  GIFT = 'gift',
  BUY_MATCHED = 'buy_matched',
  SELL_LOCK = 'sell_lock',
  SELL_UNLOCK = 'sell_unlock',
  SELL_MATCHED = 'sell_matched',
}
